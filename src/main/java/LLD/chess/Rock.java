package LLD.chess;

public class Rock extends Piece {
    public Rock(Color color, ChessPosition chessPosition) {
        super(color, chessPosition);
    }

  @Override
  public boolean canMove() {
    return super.canMove();
  }
}

