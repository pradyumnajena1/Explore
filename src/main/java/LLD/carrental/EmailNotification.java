package LLD.carrental;

import java.util.Date;

public class EmailNotification extends Notifications{
    public EmailNotification(int id, String message, Date createdOn) {
        super(id, message, createdOn);
    }
}
